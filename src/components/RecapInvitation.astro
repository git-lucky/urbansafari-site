<section class="recap-invitation wrap" data-recap-invitation hidden aria-labelledby="recap-heading">
  <div class="recap-card">
    <h2 id="recap-heading">Woo Town Safari</h2>
    <p class="recap-tagline">You made the memories. Here’s the encore.</p>
    <p>Your leaderboard, game photos, awards, and the full bonus gallery—all in one place.</p>
    <a href="https://wootown-recap.pages.dev/" class="btn btn-coral">Game highlights <span aria-hidden="true">→</span></a>
  </div>
</section>

<style>
  .recap-invitation { margin-top: clamp(3rem, 6vw, 4.5rem); }
  .recap-card {
    padding: clamp(1.5rem, 4vw, 3rem);
    border: 2px solid var(--gold);
    border-radius: var(--radius-lg);
    box-shadow: 6px 6px 0 var(--gold);
    text-align: center;
  }
  .recap-card h2 {
    font-size: clamp(3rem, 9vw, 7rem);
    line-height: 0.95;
    color: var(--gold);
    max-width: 11ch;
    margin-inline: auto;
  }
  .recap-card > p { margin: 1.25rem auto 1.75rem; color: var(--cream); max-width: 45ch; }
  .recap-card .recap-tagline { font-size: clamp(1.15rem, 2vw, 1.5rem); margin-bottom: 0.75rem; }
  .recap-card .btn { white-space: normal; justify-content: center; }
</style>
